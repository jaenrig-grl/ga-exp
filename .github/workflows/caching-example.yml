name: Cache usage example

on:
  push:

jobs:
  producer-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      
      - name: Run simply command
        run: |
          ls -la